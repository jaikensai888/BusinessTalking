# BusinessTalking baseline 研发 loop

你在 BusinessTalking 仓库工作。保留现有 Next.js + DSH runtime 架构，围绕 `docs/testing/baseline.md` 建立并执行可靠的测试，修复阻塞人格对话、多人格讨论、Skill 调用及诊断恢复的问题。目标是达到可核验的 baseline，不是尽可能多改代码。

## 开始与续跑

1. 读取适用的 AGENTS.md、`docs/testing/baseline.md`、相关源代码和已有测试。修改 Next.js 代码前读取 `node_modules/next/dist/docs/` 中的相关指南。
2. 检查 git status，保留已有用户改动、未知未跟踪文件及其他任务进程。读取 `docs/testing/loop-state.md`（若存在）和其引用的最近一批结果，先确认代码/配置是否变化。不要重新开始已经完成的工作。
3. 本提示词以通用 loop 为目标，不依赖特定插件、完成 token 或调度器。默认单 Agent；不自行创建定时任务或启动无限递归进程。
4. 第一次执行建立独立 fixture、隔离环境和结果目录。复用已有测试工具，不先搭建庞大的新框架。没有隔离数据库/DSH home、必要凭据或真实浏览器时记录具体 BLOCKED 项，继续可执行的离线工作。
5. 默认单次执行最多 5 次修复迭代、60 分钟、100 次真实模型请求；按 baseline 计数，达到任一上限即保存进度后停止。重新收到执行指令才开始下一批；不要自行重置预算继续。用户另行明确给定预算时优先遵守。

## 每轮工作

1. 根据最新结果选一个最优先失败：先错误可观测性 O02/O03/O04，再 B02/B05/S02，再其他 P0，最后 P1。可观测性正常后不反复重做。
2. 尚无结果时先跑现有确定性测试记录起点，再建立最小真实复现。按 baseline 的“单人讨论、多人讨论、通用能力”分组记录结果，读取各行优先级安排执行；原用例 ID 保持不变。为每个 baseline ID 标注需要的 A/B/C 层，缺层不能算通过。
3. 先保存失败事件和结果。区分已确认事实与假设；没有 step/start 不等于已证明某个 hook 有问题。禁止用“空回复”替代上游异常。
4. 用最小确定性回归测试锁住可定位缺陷，再修改最小范围生产代码。对于只能真实运行复现的问题，保存复现脚本、输入与原始错误，再补可行的契约测试。
5. 执行受影响测试，再执行对应真实用例和必要浏览器步骤。修复后仍失败就记录新证据；不要通过隐式重试把失败洗成成功。
6. 每轮更新该批结果及 `docs/testing/loop-state.md`，记录完成项、未通过项、代码变化、消耗、下一项。每个失败 ID 连续两轮没有新证据或有效进展，停止盲改，将该项标记 BLOCKED 并写明所需外部信息；继续其他独立可执行项。

## 必须遵守的边界

- 不迁移到 DSH WebUI、不替换框架、不无关重构、不修改上游 DSH 源码作为默认解法。若证据指向上游，提供最小复现与所需决策。
- 不删除测试、放宽断言、跳过失败项、伪造模型回复或用 mock 结果冒充真实验收；不靠每次新建 Session 绕过复用问题。
- 不静默增加自动重试、不关闭权限/审批、不把失败改成 completed、不吞掉 stderr，不以扩大超时作为未解释的修复。
- Skill 成功必须有真实工具调用与结果；多人测试必须核对每个人格的事件归属。文本相似或模型声称调用不是证据。
- 不触碰历史讨论 23SW5T 的数据；仅在隔离测试资源上故障注入、重启、归档和清理。严禁批量清空用户数据库，禁止提交凭据。
- 不执行未授权的付费服务开通、外部发信、部署或自动 Git 提交/推送。测试中的真实模型和只读搜索按本次预算运行。
- 缺少信息先查本地文件。只有影响隔离、授权或正确性且无法自行确认时，提出具体问题；记录阻塞后推进其他工作。

## 验证与终止

最终候选版本执行 `pnpm test`、`pnpm lint`、`pnpm build`，保存命令、退出码、通过/失败/跳过数量。基线已有失败与本次引入失败分开记录，但不得据此宣称全通过。真实 smoke 的 RUN_DSH_E2E 门控和命令见 baseline；完整 baseline 尚未自动化的项必须实际人工/浏览器验收或标为 BLOCKED。

按 baseline 的最终门槛验收：同一最终代码/配置全部 P0 通过；B02、B05、S02 各三个独立样本连续通过；P1 结果与影响完整列出。最终代码改动若影响之前证据，重跑相关项，不能混用旧版本证据宣称完成。

停止时只给出一种结论：

- `BASELINE_PASS`：达到上述门槛，附本批结果目录、测试摘要、P1 遗留问题。
- `BASELINE_BLOCKED`：未达到门槛且当前剩余工作需要外部信息/条件，附证据与具体所需输入。
- `BASELINE_INCOMPLETE`：到达预算或迭代上限，附未完成项和可继续的下一步。

不得因单元测试全绿、出现一条模型回复或刚好用完预算而输出 BASELINE_PASS。

`docs/testing/loop-state.md` 每次保存以下字段：批次与产物路径、代码/配置指纹、各 ID 各层状态索引、已修根因及 diff 路径、最新命令与退出码、累计迭代/耗时/真实请求数、失败连续无进展计数、剩余问题、下一步复现命令。它是续跑索引，详细证据保留在各批次目录，不能覆盖或删去旧失败。
